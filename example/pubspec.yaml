name: dpo_standard
description: A dpo_standard example project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dpo_standard:
    path: "../"
  cupertino_icons: ^0.1.3
  uuid: 3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0
flutter:

  uses-material-design: true
