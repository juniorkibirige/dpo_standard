name: example
description: A dpo_standard example project.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <=3.1.0"

dependencies:
  flutter:
    sdk: flutter
  dpo_standard:
    path: "../"
  cupertino_icons: ^1.0.8
  uuid: 3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:

  uses-material-design: true
